import { defineStore } from 'pinia'
import { ref } from 'vue'

export const recipeDetailsStore = defineStore(
  'recipeDetails',
  () => {
    const recipeDetails = ref([])
    return {
      recipeDetails
    }
  }
  //   { persist: true }
)
